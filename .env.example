# ClawGrid — copy this file to .env.local and fill in your values

# Panel authentication — set both to enable login protection
# Generate a secret: openssl rand -hex 32
PANEL_PASSWORD=change-me-to-a-strong-password
PANEL_SECRET=change-me-to-a-random-hex-string

# Leave both empty to disable auth (NOT recommended in production)
